// An invalid Rust program with syntax errors

fn main() {
    // Missing variable type and semicolon
    let x = 10
    
    // Invalid syntax for if statement (missing braces)
    if x > 5
        println!("x is greater than 5");
    
    // Unmatched closing brace
    let y: i32 = 5;
    if y < 10 {
        println!("y is less than 10");
    }}
    
    // Missing return type
    fn add(a: i32, b: i32) {
        return a + b;
    }
}
